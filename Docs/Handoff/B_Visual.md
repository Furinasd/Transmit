# B — Visual & Presentation

- Worktree: `/Users/ely/.codex/worktrees/0155/passely`; branch `Jason/visual-presentation`.
- Shared checkpoint: `bf1d0aa19c9eb377802e664cd89d0a44c82d20ef`. Assigned worktree was clean at old `49ae415`; switched only this worktree. LFS status verified with access to shared LFS scratch directory.
- Shared deadline from A: 2026-09-07 07:32 UTC; integration window starts 03:32 UTC. No reset on continuation.
- Scope: new `Source/passely/{Public,Private}/Presentation`, `Content/Transmit/Presentation`, `ContentSource/Presentation`, `Scripts/Presentation`, this handoff. A owns production map and all shared gameplay/configuration.
- Direction: ceramic white / cool graphite structure, cyan motion, warm amber high energy. Geometry and contrast first; restrained luminous edges, visible kinetic pulses and original mechanical sound punctuation.
- B1 accepted by A; B2 ready for integration (this commit).
- Interfaces: observe player Motion transaction after commit; read world-space body anchors, Ram `bArmed` / `Hits`; pre-reset clears transients, end play stops sounds. Requested public Charger dash-direction getter and optional completion readout from A.
- Editor: B used its own Editor under the agreed lease; saved and exited at 12:25 UTC. GUI returned to A. No shared MCP or generated binaries used.
- Evidence: Notion current B task + active Goal 09 read fully for current authority; existing STATE/DESIGN_CONTRACT capability tables are stale relative to bf1d0aa and not reused as runtime evidence.

## Production observations (12:02 UTC)

- Accepted A1 `58aa225` by fast-forward into B; API signature source and formal map remain A-owned.
- Original all-Content pointer scan after PIE load errors found unexpanded LFS references. `git lfs checkout` hydrated 256 tracked objects / 136 MB; fresh scan reports **zero remaining pointers**. Clean Git status alone was insufficient evidence of hydrated assets.
- First generic `EditorAssetLibrary.duplicate_asset(World)` followed by `LoadLevel` crashed UE with standalone-World GC keep-flags; no preview map file had been saved. Preserved materials/audio and switched to engine-native `LevelEditorSubsystem.new_level_from_template`, which saved `L_PresentationPreview` (199403 bytes). No broad cleanup or rollback.
- 5 dedicated materials and 8 SoundWaves saved under `/Game/Transmit/Presentation`; no Fab dependency. Source WAVs are original deterministic synthesis. New preview map is based on A1's real current map.
- B source build passed before newest surface-anchor refinement. Final visual validation is pending new B build + re-open with all LFS assets hydrated.

## B1 READY — `16b9e8d`

- 5 ceramic/graphite/motion/impact/inlay Materials and 8 original SoundWave cues, their original WAVs and deterministic generator/importer only.
- All 13 assets loaded successfully in a **fresh B Editor process after restart** (`B_ASSET_REOPEN_13_SUCCESS`, local `Saved/PresentationEvidence/Editor-hydrated.log`). Audio source validation: 48 kHz mono PCM16, -3 dBFS peak, zero clipping; exact duration/endpoint/DC checks pass.
- Import manifest: `/Game/Transmit/Presentation/{Materials,Audio}`. No extra plugin/Fab dependency. Cook requires these references to be consumed by a placed Rig (B2), or A's explicit material/audio references; presence on disk alone is not runtime use.
- A may cherry-pick `16b9e8d` now. B1 files frozen during acceptance. No preview-map, gameplay, config or Rig code in this commit.
- Runtime mix/visual aesthetic acceptance remain pending B2 preview and A integration; asset reopen is not visual or audio acceptance.

## B2 READY — presentation rig and preview

- Depends on A1 `58aa225` and B1 `16b9e8d`. Dedicated actor `ATransmitPresentationRig` reads committed Motion, Director, Charger, Ram and Reset events. No gameplay mutation. Pooled ISM strokes implement body corner housings, ownership rings, causal packets, directional output face, world motion trails, Dock cable cascade, raycast-limited dash warning, distinct impact pressure/shrapnel and completion.
- Eight referenced original spatial cues; telegraph audio stops on state exit; all owned sounds/pulses clear on Reset/EndPlay. Local retry refreshes preserved hits/armed state. Legacy arrows are hidden and lights dimmed at runtime, restored on EndPlay.
- Binary changes: new `Presentation/Maps/L_PresentationPreview.umap`; Motion, Impact, Inlay, Graphite material ISM usage flags. Formal map unchanged by B.
- A integration: load formal map; run `assemble_presentation.py` with `assemble(allow_production=True)`. Review `light_presentation.py` separately for explicit map-local lighting. Assembly resolves actor labels, never repositions gameplay. Optional ordered `Presentation_DockCable_*` anchors route cable around walls. A should classify its newer `Flow_*` frames explicitly if desired.
- Fresh Mac Editor build succeeded after final housing code (12:19 UTC). Live B preview device probe passed at 12:24:10 UTC: actual Capture/Transfer, rejected empty transfer preservation, genuine Motion-driven Dock, two genuine Charger intercepts and Ram impacts, local retry, completion staging, three clean full Resets. Evidence is staged device arrangement, **not continuous traversal**. `Saved/PresentationEvidence/probe-*.json` retains results; latest successful sequence averaged 0.0214 seconds per Slate frame, including screenshots, not a GPU benchmark.
- Actual player-camera screenshots inspected: Loaded and dash warnings visible; Ram stance still produces camera obstruction and therefore its impact screenshots do **not** establish visual acceptance. A owns the formal geometry/camera correction. Full traversal with B rig and mix/aesthetic acceptance pending A/Ely.
- Idempotent assembly ran twice on saved preview with one rig, 79 existing surfaces, no duplicated actor. Editor exited; process check confirms B GUI released.
- Reflection human gate still open: select Rig in Details, expand actor references and Cues, change SoundVolume, Save/Reopen. Python property/asset access and build do not prove Details authoring smoke. Full end-user hearing/mix acceptance also remains human-owned.

## Integrated candidate review — 2026-09-06

- B2 ready commit is `dfedd6b`; A integrated it as `9b2ec1c`. B worktree remains isolated; A owns subsequent formal-map material assignment, opening view, guide classification and cable corner references.
- Directly inspected A's `Saved/LTransmitEvidence/run-1788699031.json`: real CharacterMovement route and eligible player Interactor transactions, two Charger captures, Ram transfers/impacts, walking through the opened exit and Director completion at 65.486 game seconds. This is an automated traversal duration, not human playtime.
- Actual formal-map screenshots reviewed under A's `Saved/LTransmitEvidence/candidate-1788698965/`: opening Source, loaded state, bridge, route, Dock, high-energy state and gate sequence. Compared with `integrated-first/`, fixed-floor Inlay and Graphite guide assignment separate walking surfaces from Ceramic machinery; A routes the Dock activation cable through authored anchors and removes the old waist-height line. These are A map changes, not extra B asset writes.
- Two-hit visual evidence: `09-first-impact-04500000.png` and `09-first-impact-13000000.png` show persistent amber fracture on the closed gate; `10-final-impact-04500000.png` shows larger pressure arc and scattered shards with the opened doorway; `10-final-impact-13000000.png` shows the cleared passage after the transient fades. The western/northwestern operating view exposes the gate. The earlier east-facing-away screenshots are superseded for this read.
- Pressure arc edges remain restrained/thin; actual first-hit fracture versus second-hit debris/opening is now distinguishable in inspected frames. No global exposure increase or speculative anchor relocation was applied. Final motion feel, audio balance and aesthetic approval remain Ely's judgment.

### Asset inventory, performance and fallback

- Materials: `M_Ceramic`, `M_Graphite`, `M_Motion`, `M_Impact`, `M_Inlay` under `/Game/Transmit/Presentation/Materials`.
- Sounds: `S_Transmit_Capture`, `Transfer`, `Dock`, `Telegraph`, `Intercept`, `RamImpact1`, `RamImpact2`, `Complete` under `/Game/Transmit/Presentation/Audio` (all share the `S_Transmit_` prefix). Original deterministic WAV sources and generator are under `ContentSource/Presentation/Audio` and `Scripts/Presentation/generate_audio.py`; no external audio pack or paid redistribution dependency. Rig geometry uses the existing Engine BasicShapes Cube.
- B-owned binary delivery totals: five materials, eight SoundWaves, one preview map. B2 revised ISM usage on four of those materials; it did not modify the production map, Blueprint, input or project configuration.
- Performance evidence is limited to the successful B staged probe's 1177 Slate frames: mean 21.4 ms, median 20.9 ms, including screenshot capture. No controlled Rig-on/off GPU or memory comparison was performed; incremental GPU/CPU/memory cost is **unknown**, and these numbers are not a performance acceptance claim. Rendering allocation is bounded at 256 instances per material layer (three layers) and 16 active pulses; actual cost still requires measurement.
- Reversible authoring controls: `SoundVolume=0` mutes rig-owned audio; `EffectScale` tunes dynamic stroke size; `bReplaceLegacyPresentation=false` preserves legacy arrows/lights (may produce duplicate visual language). For a full rollback of presentation, remove the placed Rig through Editor and restore intended material assignments in a separate A-owned map transaction. None of these controls changes gameplay ownership or transactions.
- Remaining authoring smoke: A has reported Rig selection/Details visibility; full expand/representative value edit/Save/Reopen is not yet established by B's evidence. Keep that exact human gate open unless A supplies the completed surface evidence. Candidate visual review is not a claim of human acceptance or packaged-build validation.

## Juice increment — 2026-09-06, Jason/presentation-juice

Scope for this increment is the user's explicit presentation-only request. Baseline is verified local and remote `Jason/L_Transmit_v01 @ 67ed5cb`; this isolated worktree started on older `399f1e2` and was advanced to that baseline before editing. Notion Goal 09 was consulted, but its Ram assembly, ordinary trial impact and expanded encounter proposals are outside this increment's unchanged-gameplay constraint.

- Existing Rig now gives committed Capture a brief FOV contraction and Transfer a brief expansion. High Capture adds eight converging amber strokes at the actual interrupted Source. A small arrival ring closes the existing causal packet visually.
- Real first impact and final gate break receive different FOV amplitudes/durations; the larger effect still starts only from Ram's actual impact delegate. No fake defeat, time dilation, camera rotation, input lock, or new Beat is introduced.
- FOV is an additive contribution on the active player CameraComponent. It preserves the authored FOV and removes only its own contribution on full Reset, local Retry, pawn change, active-camera change and EndPlay. The existing EffectScale also attenuates camera feedback; zero suppresses this contribution. Target selection and direction resolution consume camera position/rotation, which remain untouched.
- Transient event strokes are allocated before persistent housings inside the existing bounded ISM pools, so the impact is not dropped behind static detail. No additional pool or asset dependency.
- No new reflected properties/functions/components. No map, Blueprint, material, audio, configuration or gameplay source changes; existing L2, Boss cycle, core Beat placement and flow remain the baseline's exact assets/code. This is not a claim that the baseline satisfies later Notion gameplay proposals.
- Reproduction: launch this worktree's built Editor with `-ExecutePythonScript=<absolute project path>/Scripts/Presentation/validate_juice.py`, the `/Game/Transmit/Maps/L_Transmit` map and an unused `-ModelContextProtocolPort`. The script reuses actual CharacterMovement/Interactor traversal, captures frames, then checks three full resets and one Route local retry while camera impulses are nonzero. It never saves assets. Output: `Saved/JuiceEvidence/` and `Saved/LTransmitEvidence/`.
- Fresh Mac Editor build passed. Runtime and final inventory results are recorded below after validation; human camera comfort, impact feel and audio/visual acceptance remain separate.

Validation result for the increment: fresh `passelyEditor Mac Development` build succeeded; `Saved/JuiceEvidence/Tests/index.json` reports **25 passed, zero warnings/failures/not-run**. Source-aligned PIE completed the original route, both genuine Charger captures and Ram impacts, and the exit at 66.995 game seconds (scripted traversal, not human playtime). `Saved/JuiceEvidence/runtime.json` is successful: authored FOV 90°, observed range 88.178–92.216°, one genuine Route local retry (`checkpoint=1`, completed bridge preserved) and three full resets were triggered during nonzero impulses and restored FOV to within 0.01° with zero active pulses/sounds. Local evidence log is `Saved/JuiceEvidence/final-editor.log`; actual camera frames are `Saved/LTransmitEvidence/candidate-1788704925/`. Capture, first impact and final break frames were inspected; camera comfort and dynamic/aesthetic acceptance remain human gates. Earlier startup/probe attempts are retained separately; an initial checkpoint test error was corrected in the validation route, not the gameplay code. The dedicated validation Editor exits after the run; the concurrently active main-project Editor is untouched.

Final changed-file inventory: Rig `.cpp` / `.h`, `Scripts/Presentation/validate_juice.py`, and this handoff. **Zero binary assets changed.** This work remains an uncommitted increment on `Jason/presentation-juice`; it is not merged/pushed into the shared Jason level branch. The old packaged app does not contain this increment.

## Void background increment — 2026-09-06

User requested high-ROI visual cleanup, specifically removing the black void, while retaining the earlier gameplay/layout constraints. Single-map transaction: add `Presentation_VoidMist` (`ExponentialHeightFog`) to `L_Transmit`, under the Presentation folder at Z=-250. Density .018, height falloff .12, blue-gray inscattering (.18,.27,.34), opacity 1, start distance 1200 cm; volumetric fog disabled. This fills the black lower hemisphere/horizon and retains the near-field platform/interaction contrast. No changes to existing sun, sky, exposure, material palette, geometry, gameplay or project configuration.

`Scripts/Presentation/author_void_atmosphere.py` provides an idempotent, guarded apply operation without implicit saving. The only binary change in this increment is `Content/Transmit/Maps/L_Transmit.umap`. Before/after actual player-camera frames: `Saved/VoidEvidence/before00000.png` and `after00000.png`. All 123 pre-existing actors retained their transform and actor-level collision flag. Fresh-process reopen found 124 actors with exactly one fog actor and the persisted .018 density (`reopened.json`). A reentrant preview-save callback attempted a redundant save and reported an error after the first save; fresh-process reload, rather than that callback alone, confirms the actual saved result. No rollback or unrelated asset writes.

This is one completed background pass, not a declaration of final level art quality. Human visual inspection remains open. Keep further art changes separate from this saved transaction. The shared main-project Editor/map and old packaged app are not updated by this isolated branch.

Saved-background validation passed in a fresh Editor process: Map Check 0 errors/0 warnings; original L1 → L2 → both Boss captures / Ram impacts → exit completed at 67.536 scripted game seconds; all three impulse-time full Resets and the genuine Route local retry passed. Evidence: `Saved/VoidEvidence/reopen-editor.log`, `runtime-check/runtime.json`; final-map frames `Saved/LTransmitEvidence/candidate-1788705333/`. Opening before/after and Route camera frames were visually inspected. No new native code was changed in this background increment; the earlier 25-test result remains the source regression evidence, while this run covers the changed saved map. Diff whitespace check passed.

## Simple geometry finish — 2026-09-06

User requested a more refined result using simple geometric forms and color distinction. This single map-local pass reuses `M_Ceramic`, `M_Graphite`, `M_Inlay` and Engine Cube; no new material, imported art, lighting/exposure change or runtime feature. Seven explicitly listed static floor surfaces receive graphite perimeters with large inset Inlay panels. Nine existing tall walls receive thin graphite bases/caps and sparse vertical joints. Three neutral `01/02/03` wall numbers identify progression zones. All gameplay cyan/amber materials remain unchanged. The intent is scale, architectural articulation and separation of stationary surfaces from white moving machinery, not prop density.

`Scripts/Presentation/author_geometry_finish.py` is a guarded, unsaved apply operation. It derives geometry from the current white-listed static Actor dimensions, rejects unexpected meshes/rotation, and clips inset panels against preceding overlapping floor footprints to avoid coincident overlay faces. Floor overlays top out at .7 cm, below the existing conduit; new geometry has NoCollision, no overlap events and no navigation relevance. No Gate skin, Carrier throat, stop block, hazard wall, motion guide or Ram rail is modified. Two consecutive apply calls produced identical actor inventories.

Before authoring, 124 existing actors were recorded, including transforms, actor and primitive-component collision state and protected material assignments. After the complete PIE run all protected values matched. The pass adds 131 presentation actors (128 simple cube pieces and three TextRender labels) in `Presentation/GeometryFinish`. Only seven listed original floor material assignments change. Existing transform/topology, Motion ownership, timing and core Beats are preserved. New static-piece rendering cost has not been separately benchmarked.

Real CharacterMovement/Interactor traversal completed L1 → L2 send/chase/recapture/reroute → two Charger captures and Ram impacts → opened exit. Three active-impulse full resets and a genuine Route local retry passed. Evidence: `Saved/GeometryEvidence/runtime-check/runtime.json`, `editor.log`; inspected actual-camera frames under `Saved/LTransmitEvidence/candidate-1788705833/` (opening, Route and final gate). This is automated behavioral evidence, not human art or pacing acceptance. Only `Content/Transmit/Maps/L_Transmit.umap` is a changed binary asset in this pass; no asset source filesystem operations or project settings edits. Saved to the isolated `Jason/presentation-juice` worktree; shared mainline and packaged app remain unchanged. Stop after save/reopen verification for human visual inspection.

Fresh-process saved-map verification passed (`Saved/GeometryEvidence/reopened.json`): 255 total actors, exactly 131 finish actors, all 124 protected actors retained; original component collisions and protected materials unchanged, all new cube pieces NoCollision with overlap events disabled. Map Check 0 errors/0 warnings. Final diff whitespace check passed. Human aesthetic acceptance remains open.
# Integration checkpoint — 2026-09-07

`5d6b4d9` is integrated into the pacing line based on `003d497`. The expanded map is retained and the branch's two authoring passes are replayed against its actual geometry. The formal map also now contains neutral route/operation/reclaim marks and roadside teaching signs; no original Beat or gameplay actor transform was removed.

The expanded `validate_juice.py` now walks to the relocated introductory source before active full-reset probes. Actual full traversal and all four FOV cancellations passed; 25/25 core automation passed. Final map/sign readback passed after text-only alignment/placement corrections. Human camera comfort/readability and first-play duration remain open. See `Docs/dev/20260907-ltransmit-wayfinding.md` for the exact evidence and local checkpoint scope; earlier evidence below remains historical.
